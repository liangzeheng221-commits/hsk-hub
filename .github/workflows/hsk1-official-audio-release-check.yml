name: Check HSK1 official audio release
on:
  push:
    branches:
      - agent/hsk1-official-audio-release
      - agent/hsk1-audio-step8
      - agent/hsk1-audio-architecture-cleanup
      - gh-pages
    paths:
      - 'new-hsk1/hsk1/**'
      - '.github/workflows/hsk1-official-audio-release-check.yml'
  pull_request:
    branches:
      - gh-pages
    paths:
      - 'new-hsk1/hsk1/**'
      - '.github/workflows/hsk1-official-audio-release-check.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: JavaScript syntax
        run: |
          node --check new-hsk1/hsk1/textbook-data-corrections.js
          node --check new-hsk1/hsk1/textbook-audio-segments.js
          node --check new-hsk1/hsk1/textbook-segment-audio.js
          node --check new-hsk1/hsk1/tts-only.js
          node --check new-hsk1/hsk1/textbook-audio-guard.js
          node --check new-hsk1/hsk1/audio-architecture-check.cjs
      - name: Verify static direct-routing audio architecture
        working-directory: new-hsk1/hsk1
        run: node audio-architecture-check.cjs
